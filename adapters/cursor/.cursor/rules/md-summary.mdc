---
description: 逐篇精读目录下的多个 Markdown 文件，生成一份详细的汇总/综述 Markdown
globs: ["**/*.md", "**/*.markdown"]
alwaysApply: false
---

# Markdown 文件汇总（AI 精读版）

当用户要求「汇总 Markdown」「总结多个 md 文件」「提炼多篇文档重点」「把目录里的文档合并成综述/索引」时，
使用本技能。核心是 **AI 逐篇读全文后提炼**，不是统计拼接。

规范入口：`SKILL.md`；撰写标准：`references/summarization-guide.md`（撰写摘要前必读）。

**路径约定**：`$SKILL_DIR` = 本技能根目录（skill 安装位置，例如 `~/.workbuddy/skills/markdown-file-summary`）。
所有命令用绝对路径调用，**与当前工作目录无关**；未传 `--config` 时自动使用技能自带的 `config/default.yaml`。

执行步骤：

1. `python3 "$SKILL_DIR/scripts/main.py" discover --root <目录> --out <临时目录>/manifest.json`
2. `python3 "$SKILL_DIR/scripts/main.py" extract --root <目录> --out <临时目录>/extract.json`
   （长文可改用所在工具的读文件能力直接逐篇读原文，务必读完整篇）
3. 逐篇精读原文，为每篇撰写「详细摘要 + 核心要点 + 重要细节 + 关键引用 + 结论」，再做跨文件综述，
   写入 `summaries.json`（结构见 `references/output-format.md`）。
4. `python3 "$SKILL_DIR/scripts/main.py" validate --root <目录> --manifest <临时目录>/manifest.json --summaries <临时目录>/summaries.json`
   未通过则回到第 3 步补写。
5. `python3 "$SKILL_DIR/scripts/main.py" assemble --root <目录> --manifest <临时目录>/manifest.json --summaries <临时目录>/summaries.json`
   默认输出到桌面 `~/Desktop`；用户指定位置时加 `--out <完整路径>` 或 `--out-dir <目录>`。

铁律：必须读全文；禁止把原文段落搬进摘要充当内容；禁止编造原文没有的信息。
